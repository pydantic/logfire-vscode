# Use a flat, hoisted node_modules layout (like npm/yarn classic) so the
# webpack bundle resolves the same module graph it always has — several deps
# (e.g. vscode-jsonrpc via vscode-languageclient) are imported as phantom
# dependencies and aren't reachable under pnpm's default isolated layout.
node-linker=hoisted
